### Variables
#
# Logo key. Use Mod1 for Alt.
set $mod Mod4
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l

# Your preferred terminal emulator
set $term alacritty
# Your preferred application launcher
set $menu env QT_QPA_PLATFORM=wayland QT_SCALE_FACTOR=1.25 QT_AUTO_SCREEN_SCALE_FACTOR=0 wofi --show drun | xargs swaymsg exec --

# SwayOSD needs a "server" running in the background to catch the volume signals and draw the pop-up
#exec swayosd-server i can leave this at the bottom of this config file

include ~/.config/sway/keybinds.conf
include ~/.config/sway/theme.conf


### Output configuration
#
#output * bg /home/zphish/Pictures/vector-wallpaper.png fill
output * bg /home/zphish/Pictures/wallpapers/Kath.png fill
bindsym $mod+w exec ~/.config/sway/changeWallpaper.sh


#
# Example configuration:
#
output HDMI-A-1 resolution 2560x1440@60Hz scale 1 

# HiDPI scale option (try out 1.25)
exec_always gsettings set org.gnome.desktop.interface text-scaling-factor 1.25

#
# You can get the names of your outputs by running: swaymsg -t get_outputs

### Idle configuration
#
# Example configuration:
#
 exec swayidle -w \
          timeout 900 'swaylock -f -c 000000' \
          timeout 1200 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
          before-sleep 'swaylock -f -c 000000'


#
# This will lock your screen after 900(= 15 min) seconds of inactivity, then turn off
# your displays after another 300(= 5 min) seconds, and turn your screens back on when
# resumed. It will also lock your screen before your computer goes to sleep.

### Input configuration
#
   input type:keyboard {
       xkb_layout "de"
   }
#
# set the mouse sensetivity for PixArt_Lenovo_USB_Optical_Mouse
input "type:pointer" {
    accel_profile "flat"      # optional: disables acceleration
    pointer_accel -0.5         # sensitivity, range -1 to 1
}
#
# You can also configure each device individually.
# Read `man 5 sway-input` for more information about this section.

#
#
# Utilities:
#
#
   # Special keys to adjust volume via SwayOSD (with OSD pop-ups)
   bindsym --locked XF86AudioMute exec swayosd-client --output-volume mute-toggle
   bindsym --locked XF86AudioLowerVolume exec swayosd-client --output-volume lower
   bindsym --locked XF86AudioRaiseVolume exec swayosd-client --output-volume raise
   bindsym --locked XF86AudioMicMute exec swayosd-client --input-volume mute-toggle

   # Media controls using playerctl
    bindsym --locked XF86AudioPlay exec playerctl play-pause
    bindsym --locked XF86AudioNext exec playerctl next
    bindsym --locked XF86AudioPrev exec playerctl previous	

    # Special key to take a screenshot with grim
    bindsym Print exec grim ~/Pictures/Screenshots/$(date +'%Y-%m-%d-%H%M%S').png
    # only screenshots the selected part on the screen
    bindsym Shift+Print exec grim -g "$(slurp)" ~/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png | wl-copy
    bindsym $mod+Shift+s exec grim -g "$(slurp)" ~/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png | wl-copy
#
# Status Bar:
#
bar {
    swaybar_command waybar
}

# hide the mouse cursor 
seat * hide_cursor when-typing enable
seat * hide_cursor 5000

# Start the cliphist daemon
exec wl-paste --type text --watch cliphist store
exec wl-paste --type image --watch cliphist store

# Pick from history using wofi and cliphist
bindsym $mod+i exec "cliphist list | wofi --dmenu | cliphist decode | wl-copy"

# SwayOSD needs a "server" running in the background to catch the volume signals and draw the pop-up
exec swayosd-server


##############
# window rules

assign [app_id="firefox"] workspace 2
assign [app_id="org.kde.elisa"] workspace 4
assign [app_id="org.kde.dolphin"] workspace 5

#for_window [app_id="org.kde.elisa"] floating enable 



########################################################################################################################
include /etc/sway/config.d/*
